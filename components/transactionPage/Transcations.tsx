import * as React from 'react';

interface ITransactionProps {
}

const Transaction: React.FunctionComponent<ITransactionProps> = (props):any => {
  return(
      <div>your transactions</div>
  ) ;
};

export default Transaction;
